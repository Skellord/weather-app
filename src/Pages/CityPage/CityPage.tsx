import React, { FC } from 'react';
import { Text, View } from 'react-native';

const CityPage: FC = () => {
    return (
        <View>
            <Text>City</Text>
        </View>
    );
};

export default CityPage;
